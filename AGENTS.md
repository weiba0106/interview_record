# AGENTS.md

本文件适用于仓库根目录及全部子目录。若未来某个子目录包含更具体的 `AGENTS.md`，该文件仅在对应子目录内覆盖本文件的同类规则。用户在当前任务中的明确要求始终优先。

## 1. 项目定位

本项目是一个面向秋招和日常实习求职者的多用户面试记录平台，产品形态为响应式 Web 应用，业务数据存储使用 MySQL。

当前产品基线：

- [秋招与日常实习面试记录平台 PRD](docs/superpowers/specs/2026-08-10-interview-tracker-prd-design.md)

在开始任何产品设计、技术选型或代码修改前，先阅读与任务相关的 PRD 章节。不要仅根据聊天摘要或界面截图推断需求。

## 2. 指令与事实来源

发生冲突时，按以下顺序处理：

1. 用户在当前任务中的明确要求。
2. 用户已确认的 PRD、设计文档和实施计划。
3. 当前目录及父目录中的 `AGENTS.md`。
4. 已通过的自动化测试和现有对外行为。
5. 现有代码风格与局部惯例。

如果两份已确认文档互相矛盾，停止会受影响的实现，指出具体冲突并请求用户决定。不要自行选择一种解释。

任何会改变产品行为、数据含义、权限边界或 V1 范围的请求，都应先更新对应设计文档并取得用户确认，再修改代码。纯修复、重构或等价实现不需要改动 PRD，但不得借机扩展功能。

## 3. 当前工程状态

当前尚未选择前端框架、后端框架、语言、包管理器、测试框架和部署方案。

- 在实施计划获得确认前，不要擅自初始化框架、生成脚手架或添加依赖。
- 技术选型必须以 PRD 的响应式 Web、MySQL、多用户隔离、后台提醒和邮件能力为约束。
- 首次建立工程骨架时，必须在同一变更中更新本文件的“标准命令”和“目录结构”章节。
- 不要把暂时猜测写成已经确定的技术事实。

## 4. 不可破坏的产品约束

以下规则属于系统级不变量：

- 一个用户拥有自己的公司、岗位、状态、招聘类型、面试、日程、分享和偏好数据。
- 一个公司可以包含多个岗位；一个岗位只能属于一个公司。
- 一个岗位可以包含多轮面试；每轮面试只能属于一个岗位。
- 岗位使用有顺序、可自定义且带统计分类的状态。
- 日程紧急程度同时使用颜色、图标、文字和剩余时间表达，不能只靠颜色。
- 系统提供原始靛蓝、森林青绿、暖杏棕、石墨珊瑚四套完整主题；新用户默认使用石墨珊瑚。
- 数据默认私有。未登录访问只允许通过有效分享令牌读取用户主动选择的字段。
- V1 不包含团队协作、公开经验社区、原生 App、第三方登录、外部日历同步、附件上传、AI 功能和数据导入。

若实现方案会弱化上述约束，先修改方案，不要修改约束来迎合实现。

## 5. 架构原则

在技术栈确定后，仍需保持以下边界：

- **表现层**只负责展示、交互和输入格式校验，不承载权限判断或核心业务规则。
- **应用层**编排用例、事务、授权和跨模块调用。
- **领域层**表达公司、岗位、状态、面试、日程、提醒、分享、统计和导出的业务规则。
- **基础设施层**封装 MySQL、邮件、后台任务、缓存和文件导出等外部能力。
- API、后台任务和分享页面必须复用同一套领域规则，避免三处分别实现。
- 模块通过明确接口通信。不要跨模块直接访问对方的数据表或内部实现。
- 优先创建职责单一、易测试的模块；发现文件同时承担路由、业务规则和数据访问时，应拆分职责。
- 不进行与当前任务无关的全仓库重构。

建议按业务能力划分模块，而不是按技术类型堆放全部代码：

- 认证与账号
- 公司与岗位
- 招聘类型与状态
- 面试轮次与题目
- 日程与提醒
- 分享
- 统计与导出
- 用户偏好与主题

## 6. 多用户隔离与授权

多用户数据隔离是最高优先级安全要求。

- 所有业务实体必须直接包含或可通过不可绕过的关系追溯到 `user_id`。
- 读取、更新和删除业务数据时，查询条件必须同时包含资源标识和当前用户标识；禁止只按资源 ID 查询后再依赖前端判断归属。
- 仓储或数据访问接口应要求调用方传入当前用户上下文，避免出现无租户范围的通用查询。
- 创建关联关系时，同时验证父资源和子资源属于同一用户。
- 批量操作、统计、导出、后台任务和缓存键同样必须包含用户范围。
- 权限失败统一返回安全错误，不泄露目标资源是否存在或属于谁。
- 分享接口是唯一允许未登录读取业务内容的入口，只能通过有效、未过期、未撤销的令牌访问白名单字段。

每个新增业务查询都必须有至少一个跨用户拒绝访问的测试。没有该测试，不视为完成。

## 7. MySQL 与数据变更

- 所有结构变更必须通过版本化迁移完成，禁止把手工修改数据库作为部署步骤。
- 迁移应包含必要的外键、唯一约束、非空约束和索引；不要仅依赖应用代码维持一致性。
- 为高频用户范围查询设计以 `user_id` 开头或包含 `user_id` 的合适索引。
- 时间在数据库中统一保存为 UTC，展示和提醒计算使用用户时区。
- 涉及多个实体的一致性写入必须使用事务，例如岗位删除、状态迁移、面试与日程同步。
- 删除或变更被引用的数据时，严格遵守 PRD 的阻止、迁移、归档或级联确认规则。
- 数据迁移必须考虑已有数据；不得假设表为空。
- 迁移需在干净数据库和包含代表性旧数据的数据库上验证。
- 不在日志、种子数据、测试夹具或导出文件中写入真实用户敏感数据。

## 8. 安全与隐私

- 密码只使用成熟的密码哈希库处理，不自行实现加密算法。
- 邮箱验证、密码重置和分享令牌只在生成时返回明文，数据库保存不可逆摘要。
- 会话 Cookie 必须使用 `HttpOnly`、`Secure` 和合适的 `SameSite` 设置。
- 注册、登录、重发邮件、密码重置和匿名分享访问必须实施服务端速率限制。
- 富文本在服务端按白名单清洗；客户端清洗不能替代服务端清洗。
- 投递链接和外部链接只允许安全协议，并防止 opener 劫持。
- 邮件、日志和错误报告不得包含密码、完整令牌、面试回答或复盘正文。
- 分享页面必须按照保存的字段白名单渲染，并设置禁止搜索引擎收录。
- 不提交密钥、数据库密码、邮件凭证、真实 `.env` 文件或生产数据。
- 示例配置使用 `.env.example`，只包含变量名和无敏感性的示例值。

任何涉及认证、授权、分享、富文本或删除的变更都按高风险变更处理，需要集成测试和失败路径测试。

## 9. 日程、提醒与后台任务

- 提醒任务必须幂等。重试、进程重启或重复消费不能导致同一提醒重复发送。
- 每条提醒使用稳定的唯一标识或幂等键，并持久化发送状态。
- 修改、完成或取消日程时，必须同步更新或取消尚未发送的提醒。
- 自动紧急程度严格按 PRD 的 24 小时、72 小时边界和用户时区计算。
- 用户手动设置的紧急程度优先于自动计算，清除手动值后才恢复自动计算。
- 后台任务执行时重新验证用户、日程和提醒仍然有效，不盲信入队时的旧状态。
- 邮件服务失败按照 PRD 重试，失败不能阻塞日程的正常读写。
- 使用可控时钟测试时间边界，不在测试中依赖真实等待。

## 10. API、校验与错误处理

- 前端输入始终不可信；所有业务校验和权限校验必须在服务端重复执行。
- 列表接口使用分页并设置合理上限，禁止无边界返回全部岗位或面试记录。
- 对外 API 使用一致的错误结构、字段错误格式和 HTTP 状态语义。
- 区分用户可修正错误、权限错误、资源冲突和系统错误，不把所有失败都返回为成功响应。
- 统计口径、状态分类和紧急程度由服务端定义；前端只展示结果，避免口径漂移。
- 并发编辑使用版本号或更新时间检测冲突，不静默覆盖更新较新的内容。
- 对重复请求设计安全行为，尤其是创建分享、发送提醒、删除和导出。

## 11. 前端与可访问性

- 以桌面端高密度表格为主要工作区，同时保证 375 px 宽度下可完成核心流程。
- 移动端先展示紧急日程和指标，再展示岗位记录；不要简单缩小桌面表格导致无法操作。
- 四套主题使用设计令牌实现。禁止在业务组件中散落无法切换的硬编码主题色。
- 紧急、临近、普通和已处理属于语义状态，其含义不随主题变化。
- 关键文本、焦点态和交互控件达到 WCAG 2.1 AA 对比度。
- 所有关键操作支持键盘；表单错误、状态和动态更新对辅助技术可识别。
- 加载、空数据、错误、无权限、分享失效和离线重试必须有明确界面状态。
- 不以仅悬停可见的控件承载移动端关键操作。

## 12. 测试要求

实现新功能或修复缺陷时，先编写能够证明目标行为的失败测试，再编写最小实现使其通过。

至少覆盖以下层级：

- **单元测试**：状态规则、统计公式、紧急程度、分享字段选择和纯领域逻辑。
- **集成测试**：MySQL 仓储、事务、外键、迁移、认证、权限、提醒幂等和富文本清洗。
- **端到端测试**：注册登录、创建公司岗位、记录多轮面试、创建日程、生成分享、导出和主题持久化。
- **响应式与可访问性测试**：桌面和 375 px 移动宽度的核心流程、键盘操作和语义标签。

测试规则：

- 每个受保护资源至少测试本人可访问、其他用户不可访问、未登录不可访问。
- 每个修复必须包含能在修复前失败的回归测试。
- 不通过删除、跳过或放宽有效断言来让测试通过。
- 不把网络、真实邮件服务或真实时间作为常规测试的必要条件；使用受控替身和测试时钟。
- 测试数据必须确定、隔离并在测试后清理。
- 完成声明必须附带本次实际运行的验证命令和结果，不得只说“理论上通过”。

## 13. 工作流程

每次任务按以下顺序执行：

1. 阅读当前任务、相关 PRD、适用的 `AGENTS.md` 和现有代码。
2. 检查工作区状态，识别用户已有修改；不要覆盖或回退无关变更。
3. 明确验收条件、风险和需要修改的最小范围。
4. 对多步骤实现先写计划；产品行为变化先获得设计确认。
5. 编写失败测试或可复现检查。
6. 实现最小、清晰且可维护的变更。
7. 运行针对性测试，再运行受影响范围的完整验证。
8. 检查安全、数据隔离、迁移、响应式和文档影响。
9. 汇报修改内容、验证证据、剩余风险和需要用户决定的事项。

遇到失败时先定位根因，不要连续尝试互不相关的修复。遇到权限、数据损坏或破坏性操作风险时停止并请求确认。

## 14. 代码与依赖质量

- 优先清晰、直接的实现，不为假设中的未来需求建立复杂抽象。
- 公共接口保持小而明确；命名表达业务含义，不使用含糊缩写。
- 注释解释决策原因和不明显约束，不复述代码本身。
- 不保留无归属的 `TODO`、占位实现、静默异常或永远不会执行的分支。
- 新依赖必须有明确用途，并检查维护状态、许可证、包体积或运行成本。
- 能用标准库或已有依赖可靠完成时，不新增功能重叠的包。
- 错误必须被处理、传播或记录；禁止空捕获和伪成功返回。
- 性能优化以测量结果为依据，但从一开始避免 N+1 查询、无界列表和缺少租户索引。

## 15. 文档要求

- 代码行为、数据库结构、环境变量或标准命令变化时，同一变更中更新对应文档。
- 产品需求变化更新 PRD；技术方案和取舍记录在后续架构或实施文档中。
- 不在多个文档复制大段规则；使用链接指向单一事实来源。
- 示例必须可以运行或明确标注为伪代码。
- 文档不得包含 `TBD`、未解释占位符或与当前实现不一致的命令。

## 16. Git 与文件安全

- 修改前查看 `git status`，保留用户的未提交变更。
- 只修改任务需要的文件，不格式化或重写无关文件。
- 禁止使用 `git reset --hard`、强制覆盖或批量删除来清理工作区。
- 仅在用户明确要求或已确认流程要求时创建提交。
- 提交应聚焦单一目的，提交信息说明结果而不是过程。
- 不提交构建产物、缓存、临时预览、编辑器状态或敏感配置。
- 数据库删除、迁移回滚和批量文件操作前必须确认精确目标和可恢复方式。

## 17. 标准命令与目录结构

仓库当前尚未建立应用代码，因此没有可执行的安装、开发、测试、构建或迁移命令，也没有已批准的应用目录结构。Agent 不得虚构命令。

首次工程初始化必须在同一变更中补充：

- 运行时和包管理器版本。
- 依赖安装命令。
- 本地开发命令。
- 单元、集成和端到端测试命令。
- 格式化、静态检查和类型检查命令。
- MySQL 启动、迁移、回滚和测试数据库命令。
- 生产构建与启动命令。
- 顶层目录职责和模块依赖方向。

这些内容必须来自实际可运行的工程配置，并在写入前执行验证。

## 18. 完成前检查

在声称任务完成前逐项确认：

- 实现与 PRD 和当前任务一致，没有擅自增加范围。
- 所有资源访问保持用户隔离，分享例外经过严格白名单。
- 数据库变更包含迁移、约束、索引和验证证据。
- 新增或修改行为有对应测试，失败路径也被覆盖。
- 已运行相关测试、静态检查和构建，并记录实际结果。
- 四主题、语义状态和移动端体验未被破坏。
- 日程时间、时区、提醒幂等和重试行为正确。
- 文档、示例配置和标准命令与实现同步。
- 工作区中没有意外文件、敏感信息或无关修改。

如果任何一项无法验证，应明确说明未验证内容和原因，不得将推测表述为完成事实。
